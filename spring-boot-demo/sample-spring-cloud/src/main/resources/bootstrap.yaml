spring:
  application:
    name: bar
  cloud:
    config:
      uri: http://localhost:${config.port:8080}

      server:
        git:
          uri: https://github.com/spring-cloud-samples/config-repo